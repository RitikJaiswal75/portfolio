export type ToggleButtonProps = {
    text: string;
    setActiveScreen: (screenName: string) => void;
}

export type ToggleContainerProps = {
    setActiveScreen: (screenName: string) => void;
}